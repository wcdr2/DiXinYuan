{
  "name": "gx-geo-industry-research",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build:app": "next build",
    "build": "npm run data:refresh && npm run audit:project && npm run build:app",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "data:refresh": "node scripts/update-datasets.mjs",
    "audit:project": "node scripts/check-project.mjs",
    "validate": "npm run data:refresh && npm run typecheck && npm run audit:project && npm run build:app"
  },
  "dependencies": {
    "next": "15.2.4",
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },
  "devDependencies": {
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "@types/react-dom": "19.0.4",
    "typescript": "5.8.3"
  }
}